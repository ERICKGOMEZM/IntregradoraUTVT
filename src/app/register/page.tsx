import React from 'react';
import RegisterForm from '../login/RegisterForm'; // Asegúrate de que este archivo existe

const RegisterPage = () => {
    return (
        <main className="">
            <RegisterForm /> {/* Aquí puedes agregar tu formulario de registro */}
        </main>
    );
};

export default RegisterPage;
